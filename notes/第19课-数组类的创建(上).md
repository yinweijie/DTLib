由于线性表和数组的使用方法上有些区别，因此不能完全代替数组，需要额外封装一个数组类，其类关系图为：
![](https://md-pic-1259272405.cos.ap-guangzhou.myqcloud.com/img/20200515223049.png)

1. 创建抽象类`Array`继承自`Object`：

类接口为：
```cpp
template <typename T>
class Array : public Object
{
protected:
    T* m_array;
public:
    virtual bool set(int i, const T& e);
    virtual bool get(int i, T& e) const;
    T& operator[](int i);
    T operator[](int i) const;
    virtual int length() const = 0;
};
```

类定义为：
```cpp
template <typename T>
class Array : public Object
{
protected:
    T* m_array;
public:
    virtual bool set(int i, const T& e)
    {
        bool ret = (0 <= i) && (i < length());

        if(ret)
        {
            m_array[i] = e;
        }

        return ret;
    }

    virtual bool get(int i, T& e) const
    {
        bool ret = (0 <= i) && (i < length());

        if(ret)
        {
            e = m_array[i];
        }

        return ret;
    }

    T& operator[](int i)
    {
        if((0 <= i) && (i < length()))
        {
            return m_array[i];
        }
        else
        {
            THROW_EXCEPTION(IndexOutOfBoundsException, "index out of bound...");
        }
    }

    T operator[](int i) const
    {
        return (const_cast<Array<T>*>(this))->operator[](i);
    }

    virtual int length() const = 0;
};
```

2. 创建具体的数组类`StaticArray`:

接口为：
```cpp
template <typename T, int N>
class StaticArray : public Array<T>
{
protected:
    T m_space[N];
public:
    StaticArray();
    StaticArray(const StaticArray<T, N>& rhs);
    StaticArray<T, N>& operator=(const StaticArray<T, N>& rhs);
    int length() const override;
};
```

定义为：
```cpp
template <typename T, int N>
class StaticArray : public Array<T>
{
protected:
    T m_space[N];
public:
    StaticArray()
    {
        this->m_array = m_space;
    }

    StaticArray(const StaticArray<T, N>& rhs)
    {
        this->m_array = m_space; // 这一步不能漏了，拷贝构造也是构造函数的一种，说明此时还没有创建对象，此时this->m_array还未被赋值。

        for(int i = 0; i < N; i++)
        {
            this->m_array[i] = rhs.m_space[i];
        }
    }
    StaticArray<T, N>& operator=(const StaticArray<T, N>& rhs)
    {
        if(this != &rhs)
        {
            for(int i = 0; i < N; i++)
            {
                this->m_array[i] = rhs.m_space[i];
            }
        }

        return *this;
    }

    int length() const override
    {
        return N;
    }
};
```